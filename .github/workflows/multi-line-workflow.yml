name : multi-line command
on : push
job :
  multi_line_job :
    run_on: ubuntu-latest
    steps:

    - name : checkout
      uses : actions/checkout@v4

    - name : multi-line command pass
      run : |
        echo "Hello World !!"
        ls -ltr
        cat README.md